spring:
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/shixun_test?serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8&useSSL=false
      username: root
      password: 642

  jpa:
      database: mysql
      show-sql: true
      hibernate:
        ddl-auto: update

  ##thymeleaf页面模板配置
  spring:
    mvc:
      view:
        prefix: /
        suffix: .html